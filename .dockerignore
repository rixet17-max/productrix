# Exclude everything by default, then whitelist what's needed
**

# Allow only what the API server needs
!package.json
!pnpm-workspace.yaml
!pnpm-lock.yaml
!.npmrc
!tsconfig.json
!tsconfig.base.json
!artifacts/api-server/**
!lib/**
